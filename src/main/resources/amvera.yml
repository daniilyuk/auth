meta:
  environment: java
  toolchain:
    name: maven
    version: "3.8.6"

build:
  command: mvn clean package -DskipTests

run:
  command: java -jar target/auth-0.0.1-SNAPSHOT.jar
  containerPort: 8080

network:
  routes:
    - path: /
      port: 8080

